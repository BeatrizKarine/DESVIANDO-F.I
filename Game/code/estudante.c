#include <stdio.h>
#include <char.h>
#include "nome.h"

char estudante;
int d;                             // digito para escolha do menu

    do {
    printf ("Nome:")
    scanf("%c", nome);
    }
    while {
    d = 2;
    }

return(0);
}
    

   /* #include<stdio.h>
#include<conio.h>
int main(void)
{
  char nome[61];
  
  printf("Digite seu nome: ");
  scanf("%s",nome);
  
  printf("O nome armazenado foi: %s", nome);
  
  getch();
  return 0;
}
/*
