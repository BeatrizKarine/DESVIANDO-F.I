#include <stdio.h>
#include "score.h"

int main(void){

    float nota, IA = 0;

    do {
        if (nota >= 6){
        IA += nota;
        printf("IA: score%2.f", IA);
        }
    while {
        
    }
    do {
        else {
        IA = nota - 6;
        printf("IA: score %f", IA);
        }
    while {
    }

return(0);
}
